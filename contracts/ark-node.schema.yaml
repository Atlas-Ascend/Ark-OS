schema: ga-ark-node/v1
required:
  - node_id
  - tier
  - identity
  - runtime
  - capabilities
properties:
  node_id: {type: string}
  tier: {enum: [T0, T1, T2, T3, T4, T5, T6]}
  identity: {type: string}
  runtime: {type: object}
  capabilities: {type: array}
  evidence: {type: object}
