# android-body

Capability module for **GA-ARK T2 Mobile Cyber Throne v1**.

This module is additive and may not mutate the promoted current-phone-ark baseline. Implementation must emit verifiable status and obey the command contract under `contracts/`.
