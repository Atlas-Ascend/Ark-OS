# Build Truth Index

The sixteen gates below define when GA-ARK can move from seed → verified baseline → Cyber Throne promotion. A gate marked PASS in the repository seed may still require device evidence before production promotion.
