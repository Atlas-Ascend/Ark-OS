# 13 // Deployment + Rollback

Promotion rings: local dev -> ODIN isolated -> JANUS/EDEN lab -> HYPERNET canary -> estate candidate -> canonical.

Deployments are immutable/versioned where possible. Config changes are versioned separately. Rollback is rehearsed before canonical promotion. Failed verification withholds deployment rather than silently degrading proof requirements.

VISHVARUPA remains recoverable until ARK OMEGA parity and soak criteria are sealed.
