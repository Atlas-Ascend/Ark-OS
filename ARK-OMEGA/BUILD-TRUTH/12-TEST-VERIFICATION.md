# 12 // Test + Verification Matrix

Required gates: schema validation; unit; integration; node contract conformance; transfer hash test; reconnect/resume; duplicate/replay rejection; stale credential rejection; capability denial; scheduler failover; node drain; offline queue; corrupt payload; timeout; fault injection; CLI contract; UI event fidelity; accessibility; performance budget; dependency audit; secret scan; SAST; container/image scan where applicable; SECA/DevOS acceptance.

Canary must exercise ODIN -> JANUS and ODIN -> EDEN bidirectionally plus AUTO routing through HYPERNET.
