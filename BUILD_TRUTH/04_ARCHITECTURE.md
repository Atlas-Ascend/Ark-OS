# 04 — Architecture

Components: node identity, hardware/software census, capability manifest, service supervisor, heartbeat/attestation, local policy engine, task adapter, storage/runtime registry, update/rollback controller, connectivity agent.

Nodes may differ radically in hardware but present one common contract.