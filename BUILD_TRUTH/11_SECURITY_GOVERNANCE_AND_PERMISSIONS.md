# 11 — Security, Governance, and Permissions

Node-local privilege is never implied by estate membership. Each capability declares required privilege and policy. Remote commands are authenticated and bounded. Secrets/inventory remain local or protected. Destructive actions need explicit authority; update packages/configs require integrity checks and rollback.