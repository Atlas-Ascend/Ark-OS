# Atlas Mind Runtime Boundary

This directory defines integration points with the existing phone Atlas Mind runtime. It intentionally does not vendor model weights or private runtime databases.

The current reported runtime is `atlas-mind-runtime v0.2.0`; device capture must verify the exact installed state before that declaration is promoted to build truth.
